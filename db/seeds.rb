# This file should contain all the record creation needed to seed the database with its default values.
# The data can then be loaded with the bin/rails db:seed command (or created alongside the database with db:setup).
#
# Examples:
#
#   movies = Movie.create([{ name: "Star Wars" }, { name: "Lord of the Rings" }])
#   Character.create(name: "Luke", movie: movies.first)
# Seed data for users
# db/seed.rb

# Create some users
# User.create(name: "User 1")
# User.create(name: "User 2")

# # Create categories
# Category.create(name: "Category 1", user: User.first)
# Category.create(name: "Category 2", user: User.first)
# Category.create(name: "Category 3", user: User.second)

# # Create payments
# Payment.create(name: "Payment 1", amount: 100.0, user: User.first)
# Payment.create(name: "Payment 2", amount: 50.0, user: User.first)
# Payment.create(name: "Payment 3", amount: 75.0, user: User.second)

# # Create category_payments (join table records)
# CategoryPayment.create(category: Category.first, payment: Payment.first)
# CategoryPayment.create(category: Category.first, payment: Payment.second)
# CategoryPayment.create(category: Category.second, payment: Payment.third)

# # Output a message to indicate that the seeding is complete
# puts "Seed data created successfully!"
